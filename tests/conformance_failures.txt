# Expected-failure list for the official conformance suite.
# Currently empty: the proto3 binary protobuf_test category passes fully
# (637 successes, 0 unexpected failures; JSON/text/proto2 are skipped).
# Add one test name per line if a regression appears (see
# conformance_test_runner --verbose for exact names).
#
# Known-and-accepted (NOT failures, so deliberately not listed here): the
# runner reports 14 RECOMMENDED-level
#   Recommended.Proto3.ProtobufInput.ValidDataRepeated.<TYPE>
#   .PackedInput.UnpackedOutput.ProtobufOutput
# cases as WARNING because we always emit the canonical packed encoding
# for packable repeated fields while the runner's reference encodes that
# particular fixture unpacked.  Both are legal wire encodings of the same
# message; the RECOMMENDED output-form requirement is not enforced
# (--enforce_recommended is off), and the codec intentionally keeps one
# canonical form.  Evaluated 2026-08: keep as-is.
